import "io";

fn Print(num: i32) #extern("__std_io_Print");

fn Add(x: i64, y: i64) -> i64 {
    return x + y;
}

fn Main() -> i32 {
    Print((i32) -1);
    var ans =  8 >> 1;
    Print((i32) ans);

    var a = malloc(i32)(12);

    return 0;
}
